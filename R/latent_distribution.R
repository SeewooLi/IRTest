# model.fit <- Mod1
# x <- seq(-6,6,0.05)
#
# #####################
#
# if(model.fit$Options$latent_dist=="KDE"){
#   dlatent <- outer(x, model.fit$quad, FUN = dnormal, sd=model.fit$density_par[1])%*%model.fit$Ak
# } else if(model.fit$Options$latent_dist=="DC")
#
